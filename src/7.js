// This is an example of a simple mathematical function that takes two numbers as input and returns their sum.
function add(a, b) {
  return a + b;
}

// You can call this function like so:
console.log(add(2, 3)); // Output: 5
